import ReactDOM from 'react-dom/client';
import React from 'react'
import App from './App'

// JSX jsでHTMLのような記法が使える
const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
    <App></App>
)
